Module Module1

    Sub Main()
        Dim Student_Id As Integer
        Dim Student_Name As String
        Dim Salary As Double
        Dim count1, count2 As Integer
        Dim status As Boolean
        Dim last_time, next_time As Date

        Dim PI As Double
        PI = 3.14159

        Dim Student_roll As Integer = 30
        Dim Student_grade As String = "Excellent"
    End Sub

End Module
